# After restart, install WSL
wsl --install

# Unregister Ubuntu if it exists
wsl --unregister Ubuntu

# Check available distributions
wsl -l -o

# Install the preferred distribution (replace <distro_name> with your desired distro from the list)
wsl --install -d Ubuntu-24.04

wsl --set-default-version 2

# Set up username and password during installation
